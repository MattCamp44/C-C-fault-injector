#include "./Debugger.h"

class Controller{
    private:
        // parametri conf injector
        char * name = (char *) "Debugees/Debugee1" ;
    public:
        void start(char * name);
    Controller();

};